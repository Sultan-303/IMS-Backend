name: CI

on:
  push:
    branches:
      - '**'

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up .NET
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: '8.0.100-preview.7.23376.2'
      - name: Install dependencies
        run: dotnet restore
      - name: Build
        run: dotnet build --no-restore
      - name: Run tests
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL_DEV }}
        run: dotnet test

  update-production:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up .NET SDK
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: '8.0.100-preview.7.23376.2'
      - name: Set up .NET Runtime
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: '8.0.0-preview.7.23375.6'
      - name: Install dependencies
        run: dotnet restore
      - name: Install dotnet-ef
        run: |
          dotnet tool install --global dotnet-ef --version 8.0.0-preview.7.23375.6
        env:
          DOTNET_ROOT: /home/runner/.dotnet
      - name: Apply migrations
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL_PROD }}
          DOTNET_ROOT: /home/runner/.dotnet
          PATH: "/home/runner/.dotnet:/home/runner/.dotnet/tools:$PATH"
        run: dotnet ef database update